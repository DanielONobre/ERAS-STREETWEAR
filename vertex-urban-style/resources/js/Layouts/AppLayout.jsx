// Thin wrapper — delegates to StoreLayout so existing pages keep working
// without any changes while the new CartContext / Navbar / MiniCart are active.
export { default } from '@layouts/StoreLayout';
